# Copy to `.env` and fill in real values (the real .env is git-ignored).
# Used by docker-compose.yml (Mongo root user) and the backend's Mongo connection.
MONGO_INITDB_ROOT_USERNAME=changeme
MONGO_INITDB_ROOT_PASSWORD=changeme
